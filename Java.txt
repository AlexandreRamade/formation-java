
Utiliser une méthode par défaut d'une interface à partir de la méthode redéfinie dans la classe :

class MaClasse implements MonInterface {

   public String maMethode() {
      return "DaoA" + MonInterface.super.maMethode();
   }
}



.distinct pour éliminer les doublons dans la liste !
.stream().map(order -> order.getCustomer()).distinct().collect(Collectors.toList());